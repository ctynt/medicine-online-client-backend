<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="medicine.online.client.backend.mapper.ProfessorMapper">
    <select id="selectByCategoryId" resultType="medicine.online.client.backend.model.vo.ProfessorVO">
        SELECT * FROM t_professor WHERE category_id = #{categoryId} AND delete_flag = 0;
</select>
</mapper>